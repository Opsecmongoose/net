<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login | NOZH</title>
    <link rel="stylesheet" href="/assets/css/main.css">
    <style>
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            width: 100%;
        }

        form {
            top: 0;
            position: absolute;
            left: 0;
            width: 300px;
            background: rgb(0, 0, 0, 0.6);
            height: 100%;
            padding: .5rem;
            color: #e5e5e5;
            z-index: 1;
        }

        .box-inp {
            width: 100%;
        }

        input[type="submit"] {
            background: #7a7a7a;
        }

        input[type="submit"]:hover {
            background: linear-gradient(to right, #ff3600, #d00000);
            cursor: pointer;
        }

        p {
            text-align: center;
        }

        /* Container for the iframe (GIF) background */
        .iframe-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
        }

        .iframe-container iframe {
            width: 100%;
            height: 100%;
            border: none;
            pointer-events: none; /* Disables interaction with the iframe */
        }

        @media(max-width: 400px) {
            form {
                width: 100%;
            }
        }
    </style>
</head>

<body>
    <!-- GIF Background (using GIFER iframe) -->
    <div class="iframe-container">
        <iframe src="https://gifer.com/embed/81RG" width="480" height="480" frameBorder="0" allowFullScreen></iframe>
    </div>

    <form action="/" method="post">
        <h2 class="main-t">NOZH LOGIN</h2>
        <div class="box-inp">
            <input type="text" placeholder="Username" name="username" required>
        </div>
        <div class="box-inp">
            <input type="password" placeholder="Password" name="password" required>
        </div>
        <div class="box-inp">
            <input type="submit" value="LOGIN">
        </div>
        <p>
            This tool was created by
            <a style="color: #00ff62;" href="">
                mongoose (security)
            </a>
        </p>
    </form>
</body>

</html>
